{
  "name": "letterboxd-mcp",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/letterboxd-mcp/src",
  "projectType": "application",
  "targets": {
    "typecheck": {
      "cache": true,
      "inputs": [
        "{projectRoot}/src/**/*.ts",
        "{projectRoot}/tsconfig.spec.json",
        "{projectRoot}/tsconfig.app.json",
        "{workspaceRoot}/tsconfig.base.json"
      ],
      "outputs": [],
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/letterboxd-mcp",
        "command": "tsc -p tsconfig.spec.json --noEmit"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "cache": true,
      "inputs": ["default", "^production", { "externalDependencies": [] }],
      "outputs": [],
      "options": {
        "command": "node --test",
        "cwd": "apps/letterboxd-mcp"
      }
    },
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "platform": "node",
        "outputPath": "apps/letterboxd-mcp/dist",
        "format": ["esm"],
        "bundle": true,
        "main": "apps/letterboxd-mcp/src/main.ts",
        "tsConfig": "apps/letterboxd-mcp/tsconfig.app.json",
        "assets": ["apps/letterboxd-mcp/src/assets"],
        "esbuildOptions": {
          "sourcemap": true,
          "outExtension": {
            ".js": ".js"
          },
          "banner": { "js": "#!/usr/bin/env node" }
        }
      },
      "configurations": {
        "development": {},
        "production": {
          "esbuildOptions": {
            "sourcemap": false,
            "outExtension": {
              ".js": ".js"
            },
            "banner": { "js": "#!/usr/bin/env node" }
          }
        }
      }
    },
    "prune-lockfile": {
      "dependsOn": ["build"],
      "cache": true,
      "executor": "@nx/js:prune-lockfile",
      "outputs": [
        "{workspaceRoot}/apps/letterboxd-mcp/dist/package.json",
        "{workspaceRoot}/apps/letterboxd-mcp/dist/pnpm-lock.yaml"
      ],
      "options": {
        "buildTarget": "build"
      }
    },
    "copy-workspace-modules": {
      "dependsOn": ["build"],
      "cache": true,
      "outputs": ["{workspaceRoot}/apps/letterboxd-mcp/dist/workspace_modules"],
      "executor": "@nx/js:copy-workspace-modules",
      "options": {
        "buildTarget": "build"
      }
    },
    "prune": {
      "dependsOn": ["prune-lockfile", "copy-workspace-modules"],
      "executor": "nx:noop"
    },
    "serve": {
      "continuous": true,
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "letterboxd-mcp:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "letterboxd-mcp:build:development"
        },
        "production": {
          "buildTarget": "letterboxd-mcp:build:production"
        }
      }
    }
  },
  "tags": [
    "scope:letterboxd-mcp",
    "type:app",
    "type:mcp-server",
    "publish:npm",
    "data-source:scraping"
  ]
}
